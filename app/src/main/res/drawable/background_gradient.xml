<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradiente de branco para azul claro -->
    <gradient
        android:startColor="#660000"
        android:endColor="#cc0000"
        android:angle="90" />

    <!-- Bordas arredondadas -->
    <corners android:radius="12dp" />

    <!-- Espaçamento interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />

    <!-- Borda leve -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />
</shape>
